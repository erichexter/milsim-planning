---
phase: 03-content-maps-notifications
plan: "07"
type: execute
wave: 1
depends_on: []
files_modified:
  - milsim-platform/src/MilsimPlanning.Api/Data/Migrations/20260313150605_Phase2Schema.cs
  - milsim-platform/src/MilsimPlanning.Api.Tests/Migrations/Phase2StatusMigrationTests.cs
autonomous: true
gap_closure: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, MAPS-01, MAPS-02, MAPS-03, MAPS-04, MAPS-05]

must_haves:
  truths:
    - "Content and map integration tests can initialize the database without migration type-cast errors"
    - "Applying migrations from InitialSchema through Phase3Schema succeeds in a clean PostgreSQL database"
    - "Event status values remain semantically correct after the Phase2 text-to-enum migration step"
  artifacts:
    - path: "milsim-platform/src/MilsimPlanning.Api/Data/Migrations/20260313150605_Phase2Schema.cs"
      provides: "Explicit PostgreSQL-safe conversion for Events.Status from text to integer enum values"
    - path: "milsim-platform/src/MilsimPlanning.Api.Tests/Migrations/Phase2StatusMigrationTests.cs"
      provides: "Regression test proving migration upgrade path works on a fresh database"
  key_links:
    - from: "20260313150605_Phase2Schema.cs"
      to: "Events.Status column"
      via: "ALTER COLUMN ... USING expression"
      pattern: "USING"
    - from: "Phase2StatusMigrationTests.cs"
      to: "AppDbContext.Database.MigrateAsync"
      via: "test applies real migrations in containerized PostgreSQL"
      pattern: "MigrateAsync"
---

<objective>
Close Root Cause B by fixing the Phase2 migration so PostgreSQL can convert `Events.Status` from legacy text to enum integer during test database setup.

Purpose: CONT and MAPS suites fail before endpoint behavior is exercised because migration `Phase2Schema` uses an implicit cast PostgreSQL rejects.
Output: Migration fix plus a focused regression test that locks the conversion behavior.
</objective>

<execution_context>
@C:/Users/eric/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eric/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-maps-notifications/03-VERIFICATION.md
@.planning/phases/03-content-maps-notifications/03-06-SUMMARY.md
@milsim-platform/src/MilsimPlanning.Api/Data/Migrations/20260313150605_Phase2Schema.cs
@milsim-platform/src/MilsimPlanning.Api.Tests/Maps/MapResourceTests.cs
@milsim-platform/src/MilsimPlanning.Api.Tests/Content/InfoSectionTests.cs

<interfaces>
From `milsim-platform/src/MilsimPlanning.Api/Data/Entities/Event.cs`:
```csharp
public enum EventStatus { Draft = 0, Published = 1 }
public EventStatus Status { get; set; } = EventStatus.Draft;
```

From `milsim-platform/src/MilsimPlanning.Api.Tests/Content/InfoSectionTests.cs`:
```csharp
var db = scope.ServiceProvider.GetRequiredService<AppDbContext>();
await db.Database.MigrateAsync();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Phase2 status migration PostgreSQL-safe</name>
  <files>
    milsim-platform/src/MilsimPlanning.Api/Data/Migrations/20260313150605_Phase2Schema.cs
  </files>
  <behavior>
    - Migrating from `InitialSchema` to `Phase2Schema` no longer throws `42804` for `Events.Status`.
    - Existing text values (`draft`, `published`) map to enum ints (`0`, `1`) deterministically.
    - The migration remains idempotent for fresh test databases and retains the existing schema intent.
  </behavior>
  <action>
    Update `Phase2Schema.Up` to replace the implicit `AlterColumn` cast with a PostgreSQL-compatible conversion path:
    - Drop the legacy default on `Events.Status` first.
    - Run SQL that converts string values to integer enum values with an explicit `USING CASE` expression.
    - Re-apply integer default (`0`) and keep nullability unchanged.

    Keep DateOnly conversions and other table additions unchanged. Do not introduce a new migration file; patch the existing `Phase2Schema` migration because test fixtures replay historical migrations from scratch.
  </action>
  <verify>
    <automated>dotnet test milsim-platform/milsim-platform.slnx --filter "FullyQualifiedName~Phase2StatusMigrationTests"</automated>
  </verify>
  <done>
    `Phase2Schema` migrates cleanly in PostgreSQL and no `column "Status" cannot be cast automatically to type integer` error is emitted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add migration regression test and prove CONT/MAPS suites bootstrap</name>
  <files>
    milsim-platform/src/MilsimPlanning.Api.Tests/Migrations/Phase2StatusMigrationTests.cs
  </files>
  <behavior>
    - A test explicitly exercises migration replay on a clean PostgreSQL instance.
    - The test asserts the upgraded schema uses integer status and can persist/read `EventStatus` values.
    - CONT and MAPS category runs no longer fail in `InitializeAsync` due to migration setup.
  </behavior>
  <action>
    Create `Phase2StatusMigrationTests.cs` using the existing Testcontainers fixture pattern:
    - Build a minimal `WebApplicationFactory` against `_fixture.ConnectionString`.
    - Call `db.Database.MigrateAsync()` and assert completion without exception.
    - Insert/read an `Event` row with `EventStatus.Draft` and assert persisted enum value remains valid.

    Then run the category regression command for content/maps only to confirm setup paths that previously failed in `InfoSectionTestsBase.InitializeAsync` and `MapResourceTestsBase.InitializeAsync` now pass migration stage.
  </action>
  <verify>
    <automated>dotnet test milsim-platform/milsim-platform.slnx --filter "Category~CONT|Category~MAPS"</automated>
  </verify>
  <done>
    Migration regression test passes and CONT/MAPS suites execute behavior tests instead of stopping at migration bootstrap.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test milsim-platform/milsim-platform.slnx --filter "FullyQualifiedName~Phase2StatusMigrationTests"` passes.
2. `dotnet test milsim-platform/milsim-platform.slnx --filter "Category~CONT|Category~MAPS"` no longer reports Postgres 42804 cast errors.
3. `20260313150605_Phase2Schema.cs` contains explicit SQL conversion for `Events.Status`.
</verification>

<success_criteria>
- Root Cause B is closed: migration replay succeeds for content/maps fixtures.
- CONT and MAPS requirement test categories can start and run endpoint assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-maps-notifications/03-07-SUMMARY.md`
</output>
