---
phase: 03-content-maps-notifications
plan: "06"
type: execute
wave: 4
depends_on: [03-04, 03-05]
files_modified:
  - milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationJob.cs
  - milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationWorker.cs
  - milsim-platform/src/MilsimPlanning.Api/Controllers/RosterChangeDecisionsController.cs
  - milsim-platform/src/MilsimPlanning.Api/Models/Notifications/QueueRosterDecisionRequest.cs
  - milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
autonomous: true
gap_closure: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, MAPS-01, MAPS-02, MAPS-03, MAPS-04, MAPS-05, NOTF-01, NOTF-02, NOTF-03, NOTF-04, NOTF-05]

must_haves:
  truths:
    - "When a commander records an approve/deny roster decision, a notification job is queued immediately and API returns 202"
    - "NotificationWorker sends roster decision emails through transactional provider using the same async queue pipeline as blast/squad emails"
    - "Roster decision email content includes decision outcome and enough context for the player to understand what changed"
    - "Existing content/maps/notification behaviors remain green after the NOTF-03 fix"
  artifacts:
    - path: "milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationJob.cs"
      provides: "RosterChangeDecisionJob contract added to notification job model"
    - path: "milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationWorker.cs"
      provides: "Worker branch that processes roster decision jobs with Resend EmailSendAsync"
    - path: "milsim-platform/src/MilsimPlanning.Api/Controllers/RosterChangeDecisionsController.cs"
      provides: "Commander decision endpoint that enqueues roster decision notifications"
    - path: "milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs"
      provides: "NOTF-03 integration/worker assertions plus phase-3 regression guards"
  key_links:
    - from: "RosterChangeDecisionsController.cs"
      to: "INotificationQueue.EnqueueAsync"
      via: "enqueue RosterChangeDecisionJob after event scope validation"
      pattern: "EnqueueAsync.*RosterChangeDecisionJob"
    - from: "NotificationWorker.cs"
      to: "IResend.EmailSendAsync"
      via: "ProcessRosterChangeDecisionAsync branch"
      pattern: "RosterChangeDecisionJob.*EmailSendAsync"
    - from: "NotificationTests.cs"
      to: "phase 3 requirements"
      via: "CONT/MAPS/NOTF category regression run"
      pattern: "Category=CONT|Category=MAPS|Category=NOTF"
---

<objective>
Close the Phase 3 verification gap for NOTF-03 by adding roster-change-decision notification wiring to the existing async notification pipeline.

Purpose: Phase 3 verification failed because roster decision emails were explicitly deferred. This plan adds the missing job contract, worker handling, and an enqueue point in an approval/denial flow endpoint so NOTF-03 is actually executable now.
Output: Roster decision queue path (controller -> queue -> worker -> transactional provider) plus targeted tests and a regression run over all Phase 3 requirement categories.
</objective>

<execution_context>
@C:/Users/eric/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eric/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-maps-notifications/03-CONTEXT.md
@.planning/phases/03-content-maps-notifications/03-RESEARCH.md
@.planning/phases/03-content-maps-notifications/03-VERIFICATION.md
@.planning/phases/03-content-maps-notifications/03-04-SUMMARY.md
@milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationJob.cs
@milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationWorker.cs
@milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs

<interfaces>
From `milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationJob.cs`:
```csharp
public abstract record NotificationJob;
public record BlastNotificationJob(...) : NotificationJob;
public record SquadChangeJob(...) : NotificationJob;
```

From `milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationWorker.cs`:
```csharp
switch (job)
{
    case BlastNotificationJob blastJob:
        await ProcessBlastAsync(...);
        break;
    case SquadChangeJob squadChangeJob:
        await ProcessSquadChangeAsync(...);
        break;
}
```

From `milsim-platform/src/MilsimPlanning.Api/Controllers/NotificationBlastsController.cs` (pattern to reuse):
```csharp
ScopeGuard.AssertEventAccess(_currentUser, eventId);
await _notificationQueue.EnqueueAsync(new BlastNotificationJob(...));
return Accepted(new { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend notification contracts and worker for roster decision delivery</name>
  <files>
    milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationJob.cs,
    milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationWorker.cs,
    milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
  </files>
  <behavior>
    - NotificationJob adds `RosterChangeDecisionJob` with recipient, event name, decision outcome (approved/denied), and commander note fields.
    - NotificationWorker switch handles `RosterChangeDecisionJob` and sends exactly one transactional email per job via `IResend.EmailSendAsync`.
    - Decision email HTML includes recipient name, decision status, and clear action context (approved/denied and summary text).
    - Existing blast and squad-change behavior stays unchanged.
  </behavior>
  <action>
    Update `NotificationJob.cs` to remove the Phase 4 defer comment and add a real `RosterChangeDecisionJob : NotificationJob` record.

    Update `NotificationWorker.cs`:
    - Add `case RosterChangeDecisionJob rosterDecisionJob` to the existing job switch.
    - Implement `ProcessRosterChangeDecisionAsync(RosterChangeDecisionJob job, IResend resend, IConfiguration config, CancellationToken ct)`.
    - Add `BuildRosterDecisionHtml` helper that renders approved and denied outcomes with distinct messaging.
    - Keep the existing scoped DI pattern (`CreateAsyncScope`) and do not inject scoped services into the worker constructor.

    Add/extend tests in `NotificationTests.cs` for worker-level NOTF-03 behavior:
    - `RosterDecisionJob_Approved_SendsEmail`
    - `RosterDecisionJob_Denied_SendsEmail`
    - Assert email subject/body contains decision outcome and recipient context.
  </action>
  <verify>
    <automated>dotnet test milsim-platform/milsim-platform.slnx --filter "Category=NOTF_Decision_Worker"</automated>
  </verify>
  <done>
    `RosterChangeDecisionJob` exists, worker handles it, and NOTF decision worker tests pass without regressing blast/squad paths.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add commander decision enqueue endpoint and close NOTF-03 verification gap</name>
  <files>
    milsim-platform/src/MilsimPlanning.Api/Controllers/RosterChangeDecisionsController.cs,
    milsim-platform/src/MilsimPlanning.Api/Models/Notifications/QueueRosterDecisionRequest.cs,
    milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
  </files>
  <behavior>
    - Commander-only endpoint accepts approval/denial decision payload and returns `202 Accepted` after queueing.
    - Endpoint validates event scope (`ScopeGuard.AssertEventAccess`) and player membership before enqueue.
    - Enqueued payload is a `RosterChangeDecisionJob` consumed by the worker path from Task 1.
    - Notification test suite proves queue enqueue for approve and deny cases and blocks unauthorized player attempts.
  </behavior>
  <action>
    Create `QueueRosterDecisionRequest.cs` DTO with required fields:
    - `EventPlayerId` (Guid)
    - `Decision` (`approved` or `denied`; reject anything else with 400)
    - `RequestedChangeSummary` (string, required)
    - `CommanderNote` (string?, optional)

    Create `RosterChangeDecisionsController.cs` route:
    - Base route: `api/events/{eventId:guid}/roster-change-decisions`
    - `POST` action with `[Authorize(Policy = "RequireFactionCommander")]`
    - Perform `ScopeGuard.AssertEventAccess(_currentUser, eventId)`
    - Load target `EventPlayer` by `{eventId, eventPlayerId}`; return 404 if not found
    - If `EventPlayer.UserId` or `Email` is null, return 422 with actionable error (cannot send decision email to unregistered player)
    - Enqueue `RosterChangeDecisionJob` and return `Accepted(new { queued = true })`

    Extend `NotificationTests.cs` with API-level NOTF-03 tests:
    - `QueueRosterDecision_Approved_Returns202_AndEnqueuesJob` (Category=NOTF_Decision)
    - `QueueRosterDecision_Denied_Returns202_AndEnqueuesJob` (Category=NOTF_Decision)
    - `QueueRosterDecision_PlayerRole_Forbidden` (Category=NOTF_Decision)

    Close the phase gap with a regression verification run that protects all phase requirements after this fix (CONT, MAPS, NOTF categories).
  </action>
  <verify>
    <automated>dotnet test milsim-platform/milsim-platform.slnx --filter "FullyQualifiedName~QueueRosterDecision_"</automated>
  </verify>
  <done>
    Commander approval/denial enqueue endpoint returns 202, queue receives `RosterChangeDecisionJob` for valid requests, player role is forbidden, and full phase category regression passes.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test milsim-platform/milsim-platform.slnx --filter "Category=NOTF_Decision_Worker|Category=NOTF_Decision"` passes.
2. `dotnet test milsim-platform/milsim-platform.slnx --filter "Category=CONT|Category=MAPS|Category=NOTF"` passes (Phase 3 regression guard).
3. `NotificationJob.cs` contains concrete `RosterChangeDecisionJob` (no defer comment).
4. `NotificationWorker.cs` has explicit `RosterChangeDecisionJob` switch branch and `EmailSendAsync` call.
5. `RosterChangeDecisionsController.cs` enqueues after scope and membership checks and returns HTTP 202.
</verification>

<success_criteria>
- Verification gap from `03-VERIFICATION.md` (truth #5 / NOTF-03) is fully wired: job contract, enqueue point, worker delivery path.
- NOTF-03 tests are present and passing for approve, deny, and unauthorized scenarios.
- Existing CONT/MAPS/NOTF behavior remains green after the fix.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-maps-notifications/03-06-SUMMARY.md`
</output>
