---
phase: 03-content-maps-notifications
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
autonomous: true
gap_closure: true
requirements: [NOTF-01, NOTF-02, NOTF-03, NOTF-04, NOTF-05]

must_haves:
  truths:
    - "Notification integration tests boot the API host without IServiceProvider disposal failures"
    - "Notification blast, squad-change, and roster-decision queue API tests can execute their endpoint assertions"
    - "Notification worker behavior tests remain valid for approved/denied decision delivery"
  artifacts:
    - path: "milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs"
      provides: "Stable notification test host setup with deterministic queue behavior"
  key_links:
    - from: "NotificationTestsBase.InitializeAsync"
      to: "WebApplicationFactory service provider lifetime"
      via: "test DI overrides for queue/hosted services"
      pattern: "ConfigureServices"
    - from: "NotificationBlastTests / SquadChangeNotificationTests / RosterDecisionQueueApiTests"
      to: "INotificationQueue.EnqueueAsync"
      via: "Moq verification on queued jobs"
      pattern: "Verify\(q => q\.EnqueueAsync"
---

<objective>
Close Root Cause A by stabilizing notification test host setup so the NOTF suite no longer fails at initialization with disposed service provider errors.

Purpose: Notification tests currently crash before exercising API behavior because the hosted worker + mocked queue combination destabilizes DI lifetime during test startup.
Output: Hardened notification test setup and passing NOTF category regression.
</objective>

<execution_context>
@C:/Users/eric/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/eric/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-maps-notifications/03-VERIFICATION.md
@.planning/phases/03-content-maps-notifications/03-06-SUMMARY.md
@milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
@milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/NotificationWorker.cs
@milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/INotificationQueue.cs

<interfaces>
From `milsim-platform/src/MilsimPlanning.Api/Infrastructure/BackgroundJobs/INotificationQueue.cs`:
```csharp
public interface INotificationQueue
{
    ValueTask EnqueueAsync(NotificationJob job, CancellationToken ct = default);
    IAsyncEnumerable<NotificationJob> ReadAllAsync(CancellationToken ct);
}
```

From `milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs`:
```csharp
_queueMock.Verify(q => q.EnqueueAsync(
    It.IsAny<BlastNotificationJob>(),
    It.IsAny<CancellationToken>()), Times.Once);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden notification test host bootstrapping</name>
  <files>
    milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
  </files>
  <behavior>
    - Notification API test classes initialize without `System.ObjectDisposedException: IServiceProvider`.
    - Test host does not terminate early from background worker startup side effects.
    - Existing enqueue verifications for blast, squad, and roster decision tests remain intact.
  </behavior>
  <action>
    Update `NotificationTestsBase.InitializeAsync` to make the queue/hosted-service setup deterministic in tests:
    - Ensure the mocked `INotificationQueue` supplies a safe `ReadAllAsync` implementation (non-throwing, cancellation-aware, no immediate host-fatal exceptions).
    - Remove or neutralize hosted background worker registrations in this test factory so API endpoint tests focus on controller/service behavior and queue enqueue contracts.
    - Keep `_queueMock.Verify(...)` assertions unchanged across API tests.

    Do not change production notification worker code in this plan.
  </action>
  <verify>
    <automated>dotnet test milsim-platform/milsim-platform.slnx --filter "Category~NOTF_Blast|Category~NOTF_Squad|Category~NOTF_Decision"</automated>
  </verify>
  <done>
    Notification API tests run and assert queue enqueue behavior without disposed-service-provider startup failures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-run notification worker + regression categories</name>
  <files>
    milsim-platform/src/MilsimPlanning.Api.Tests/Notifications/NotificationTests.cs
  </files>
  <behavior>
    - Worker unit-style tests for roster decision approved/denied continue to pass.
    - Category-wide NOTF run validates both worker and API queue paths after test harness changes.
    - No new failures are introduced in NOTF requirement coverage.
  </behavior>
  <action>
    Keep and execute both scopes of notification verification:
    - Worker behavior tests (`NOTF_Decision_Worker`) for `ProcessRosterChangeDecisionAsync` expectations.
    - Full NOTF category run (`NOTF_Blast`, `NOTF_Squad`, `NOTF_Decision`, `NOTF_Decision_Worker`) to confirm end-to-end queue contracts in tests.

    If any assertion broke because of harness changes, adjust only test setup plumbing (not product behavior) so tests remain behavior-focused.
  </action>
  <verify>
    <automated>dotnet test milsim-platform/milsim-platform.slnx --filter "Category~NOTF"</automated>
  </verify>
  <done>
    NOTF category tests pass with stable initialization and existing NOTF-01..05 assertions preserved.
  </done>
</task>

</tasks>

<verification>
1. `dotnet test milsim-platform/milsim-platform.slnx --filter "Category~NOTF"` passes.
2. Notification tests no longer fail in `NotificationTestsBase.InitializeAsync` with `ObjectDisposedException`.
3. Queue enqueue assertions for blast/squad/decision API tests remain green.
</verification>

<success_criteria>
- Root Cause A is closed: notification test initialization is stable.
- NOTF-01..05 regression tests execute to completion and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-maps-notifications/03-08-SUMMARY.md`
</output>
